import { Logger } from "pino";
import { JsonRpcPayload } from "@walletconnect/jsonrpc-types";
import { IClient } from "./client";
import { IRelayerEncoder } from "./relayer";
export declare abstract class IEncoder implements IRelayerEncoder {
    client: IClient;
    logger: Logger;
    abstract name: string;
    abstract readonly context: string;
    constructor(client: IClient, logger: Logger);
    abstract encode(topic: string, payload: JsonRpcPayload): Promise<string>;
    abstract decode(topic: string, encrypted: string): Promise<JsonRpcPayload>;
}
//# sourceMappingURL=encoder.d.ts.map