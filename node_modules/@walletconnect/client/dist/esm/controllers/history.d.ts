/// <reference types="node" />
import { EventEmitter } from "events";
import { Logger } from "pino";
import { IJsonRpcHistory, JsonRpcRecord, RequestEvent, IRelayerStorage } from "@walletconnect/types";
import { JsonRpcRequest, JsonRpcResponse } from "@walletconnect/jsonrpc-utils";
export declare class JsonRpcHistory extends IJsonRpcHistory {
    logger: Logger;
    storage: IRelayerStorage;
    records: Map<number, JsonRpcRecord>;
    events: EventEmitter;
    name: string;
    private cached;
    constructor(logger: Logger, storage: IRelayerStorage);
    init(): Promise<void>;
    get context(): string;
    get size(): number;
    get keys(): number[];
    get values(): JsonRpcRecord[];
    get pending(): RequestEvent[];
    set(topic: string, request: JsonRpcRequest, chainId?: string): Promise<void>;
    resolve(response: JsonRpcResponse): Promise<void>;
    get(topic: string, id: number): Promise<JsonRpcRecord>;
    delete(topic: string, id?: number): Promise<void>;
    exists(topic: string, id: number): Promise<boolean>;
    on(event: string, listener: any): void;
    once(event: string, listener: any): void;
    off(event: string, listener: any): void;
    removeListener(event: string, listener: any): void;
    private getRecord;
    private persist;
    private restore;
    private initialize;
    private reset;
    private onInit;
    private isInitialized;
    private registerEventListeners;
}
//# sourceMappingURL=history.d.ts.map